<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xigua.client.mapper.FriendRequestMapper">
    <select id="getSendCountByUserId" resultType="java.lang.Integer">
        select count(*) from xg_friend_request where del_flag = 0 and sender_id = #{userId}
    </select>
    <select id="getReceiveCountByUserId" resultType="java.lang.Integer">
        select count(*) from xg_friend_request where del_flag = 0 and receiver_id = #{userId}
    </select>
</mapper>
